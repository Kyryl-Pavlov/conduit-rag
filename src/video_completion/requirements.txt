# boto3 is provided by the Lambda runtime; no extra third-party dependencies
# needed -- same tier as transcribe_completion's requirements.txt. common/
# ships as a separate Lambda Layer, not bundled into this function's zip.
